class Taskss {
  String id;
  String subtitle;
  String title;
  DateTime createdAtTime;
  DateTime createdAtDate;
  bool isDone;

  Taskss(this.id, this.subtitle, this.title, this.createdAtTime,
      this.createdAtDate, this.isDone);
}
