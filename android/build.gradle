<<<<<<< HEAD
=======
buildscript {
    repositories {
        google() // Đảm bảo Google repository được thêm
        mavenCentral() // Đảm bảo Maven Central repository được thêm
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.2' // Plugin Gradle
        classpath 'com.google.gms:google-services:4.3.15' // Plugin Google Services
    }
}

>>>>>>> f786b41 (update project final)
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
